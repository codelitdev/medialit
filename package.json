{
  "name": "medialit",
  "private": true,
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "@typescript-eslint/eslint-plugin": "^5.17.0",
    "@typescript-eslint/parser": "^5.17.0",
    "eslint": "^8.12.0",
    "husky": ">=6",
    "lint-staged": ">=10",
    "prettier": "^2.6.1",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "prepare": "husky install",
    "ci:publish": "changeset tag && pnpm publish -r"
  },
  "lint-staged": {
    "*.ts": "eslint --cache --fix",
    "*.{ts,js,css,md}": "prettier --write"
  }
}
